#class {heartbeat} {kill}
#class {heartbeat} {open}

#var make_con 1;

#alias {_player_heartbeat} {
	#if {!$idle_flag && $make_con && $my[worth] < 125} {con 100;#var make_con 0;#delay 4 {#var make_con 1}} {#NOP};
};

#class {heartbeat} {close}
