#CLASS {vars} OPEN

#var username zorbaine;
#var password qazokm;

#VARIABLE         {curvnum}  {4119}
#VARIABLE         {shield}   {aurothon shield}
#VARIABLE         {wielded}  {$wielded}

#var map[height] 20;
#var map[width] 60;

#var hpbar[height] 4;
#var hpbar[left] 0;

#VARIABLE {draw}  {
    {canvas}{on}
    {caption}{on}
    {chat}{on}
    {gline}{on}
    {map}{on}
	{hpbar}{on}
};  

#CLASS {vars} CLOSE
