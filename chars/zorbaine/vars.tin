#CLASS {vars} OPEN

#var username zorbaine;
#var password qazokm;

#VARIABLE         {curvnum}  {4119}
#VARIABLE         {shield}   {aurothon shield}
#VARIABLE         {wielded}  {$wielded}


#VARIABLE {draw} {
    {canvas}{on}
    {caption}{off}
    {chat}{off}
    {gline}{off}
    {map}{on}
	{hpbar}{on}
    {room}{on}
}

#var canvas[widthperc] .5

#var map[widthperc] .5; /* must be <= canvas[widthperc] */
#var map[heightperc] .6;

#var hpbar[height] 3;
#var hpbar[left] 0;

#var room[widthperc] .5; /* must be <= canvas[widthperc] */
#var room[heightperc] .4;

#CLASS {vars} CLOSE
